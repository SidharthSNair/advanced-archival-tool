fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
alembic>=1.13
pydantic>=2.8
python-dotenv>=1.0
psycopg[binary]>=3.2      # for Postgres later (safe to keep; unused on SQLite)
orjson>=3.10              # faster JSON responses
loguru>=0.7               # nice logging
tenacity>=8.5             # retries (db / fs operations)
tzdata>=2024.1            # timezone data on Windows
